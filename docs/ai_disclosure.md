# AI Assistance Disclosure

**Required by §6 of the brief**

> *"AI assistants are permitted and expected. Disclose where you used them in
> a short appendix. You own every line you submit — expect to be asked in the
> demo to explain any part of your code from memory, and 'the model wrote it'
> is not an answer."*

---

## Summary

**AI assistance was used extensively on this project.** Claude (Anthropic)
was used across code, documentation, and project management. The table below
is deliberately detailed rather than a one-line acknowledgement — §6 treats
under-disclosure as an integrity problem, and over-disclosing costs nothing.

The short version: **the team made the decisions and ran the experiments; the
AI wrote a large share of the code and prose that implements and describes
them.** Where that split is less clean, the table says so.

---

## What AI assistance produced

| Area | What it did | Owner |
|---|---|---|
| **The notebook** (`ML_FinalProject_Group_8.ipynb`) | Code scaffolding, debugging and drafting throughout, per the disclosure in the notebook itself. The experimental design, the runs, and the decisions about what to try were the team's | Thenmani |
| **`anpr_package/` structure** | Proposed the `src/anpr/` layout; created the module skeleton and all docstrings | Luis |
| **Data-layer modules** | Ported the Week-1 notebook code (loader, orientation guard, splits, input contract) from notebook cells into modules. Logic was the team's; the restructuring into functions with parameters was AI | Thenmani / Luis |
| **Segmentation + plate generator modules** | Ported Thenmani's notebook implementations into `segment/` and `data/plates.py`. Added cross-platform font discovery (the original was Linux-only) | Luis |
| **Test suite** (53 tests) | Written by AI, covering the input contract, orientation guard, label handling, metric counting rules, cost model, and the EMNIST truncation guard | Luis |
| **Spike script** (`spike_segmentation.py`) | Written by AI. The experimental question (does connected-component segmentation survive tight kerning?) and the interpretation were discussed with the team | Luis |
| **QA documents** | `qa_acceptance_criteria.md` (68 Gherkin criteria) and `qa_execution_log.md` drafted by AI; the pass/fail determinations were made by running the code | Luis |
| **Written deliverables** | `approach.md`, `spike.md`, `results.md`, `business_note.md` drafted by AI from the team's measured results. **Every number in them comes from executed code, not from the model** | Valentina / Luis |
| **Bug fixes found by AI** | TensorFlow import ordering in `load_reader()`; EMNIST route order (expired NIST certificate); `PROJECT_ROOT` path depth after the restructure; the business note contradicting its own sensitivity table | Luis |
| **Jira administration** | Ticket transitions and evidence comments | Luis |
| **Repository docs** | `README.md`, `CLAUDE.md`, folder READMEs | Luis |

---

## What was *not* AI-generated

Stated because the distinction matters more than the disclosure itself:

- **Every measured number.** All accuracy figures, the confusion analysis,
  the tier results and the business case come from executing code on real
  data. None were produced or estimated by a language model.
- **The trained models.** `plate_cnn.keras` and `plate_mlp.keras` are the
  output of training runs the team executed in Colab.
- **The experimental decisions** — which spike questions to test, which
  dataset to use, the case-merge and class-weighting choices, and the
  decision to consolidate on a single notebook.
- **The data.** EMNIST is a published dataset; the synthetic plates were
  generated by code in this repository.

---

## ⚠️ Outstanding: nobody has signed off

The **Owner** column above names who *should* own each item — not who has
confirmed they can defend it. That confirmation has not happened yet, and §6
is unambiguous that it is the thing being graded:

> *"You own every line you submit — expect to be asked in the demo to explain
> any part of your code from memory, and 'the model wrote it' is not an
> answer."*

**Before submission, each person should read the code and documents attributed
to them above and either be able to explain them, or change them until they
can.** A section you cannot defend live is worth less than a shorter one you
wrote yourself.

---

## The from-memory checklist

§8 promises these get asked. Each owner should answer without notes:

| Area | Question |
|---|---|
| **Data** | Why does EMNIST need a transpose, and how does the guard *prove* it fired rather than just claiming to check? |
| **Model** | Layer count, learning rate, why `use_bias=False` before BatchNorm, and the train/validation/test sizes |
| **Pipeline** | Why are crops centred by *centre of mass* in a 20×20 box inside a 28×28 field, rather than by bounding-box centre? |
| **Segmentation** | What does the v2 splitting step do, and why did v1 discard merged glyphs instead of splitting them? |
| **QA** | Why is a wrong character *count* excluded from character accuracy? |
| **Business** | Why is plate confidence the minimum and not the mean — and why does the optimal threshold not move when the misread cost changes? |

The last one is worth rehearsing specifically: the answer is that below 0.98
confidence precision falls to ~50%, so automated error costs swamp any labour
saving at any plausible misread price. That is a real finding, and being able
to explain it unprompted is the difference between reporting a number and
understanding it.
